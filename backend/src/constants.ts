import { Word } from './types.js';

// Word list for random selection
export const WORDS: Word[] = ['cat', 'dog', 'mouse', 'horse', 'fox'];

// Interval for word updates (in milliseconds)
export const WORD_UPDATE_INTERVAL_MS = 5000; // 5 seconds

